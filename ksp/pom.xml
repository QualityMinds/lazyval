<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.qualityminds.lazyval</groupId>
        <artifactId>lazyval-parent</artifactId>
        <version>0.2.2</version>
    </parent>

    <artifactId>lazyval-ksp</artifactId>
    <name>Lazyval Kotlin-Symbol-Processor</name>
    <description>KSP2 implementation of the Lazyval annotation processing</description>

    <properties>
        <kotlin.compiler.incremental>true</kotlin.compiler.incremental>
        <!-- use dokka instead of javadoc for this module -->
        <maven.javadoc.skip>true</maven.javadoc.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.qualityminds.lazyval</groupId>
            <artifactId>lazyval</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.qualityminds.lazyval</groupId>
            <artifactId>lazyval-utils</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-stdlib</artifactId>
            <version>${version.kotlin}</version>
        </dependency>
        <dependency>
            <groupId>com.google.devtools.ksp</groupId>
            <artifactId>symbol-processing-api</artifactId>
            <version>${version.ksp}</version>
        </dependency>
        <dependency>
            <groupId>com.squareup</groupId>
            <artifactId>kotlinpoet-ksp</artifactId>
            <version>${version.kotlinpoet}</version>
        </dependency>
        <dependency>
            <groupId>com.palantir.javapoet</groupId>
            <artifactId>javapoet</artifactId>
            <version>${version.javapoet}</version>
        </dependency>
        <dependency>
            <groupId>org.jetbrains</groupId>
            <artifactId>annotations</artifactId>
            <version>${version.jetbrains-annotations}</version>
            <scope>provided</scope>
        </dependency>
        <!-- Tests -->
        <dependency>
            <groupId>com.qualityminds.lazyval</groupId>
            <artifactId>lazyval-testkit</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.groovy</groupId>
            <artifactId>groovy</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.spockframework</groupId>
            <artifactId>spock-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <sourceDirectory>${project.basedir}/src/main/kotlin</sourceDirectory>
        <plugins>
            <plugin>
                <artifactId>kotlin-maven-plugin</artifactId>
                <groupId>org.jetbrains.kotlin</groupId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.gmavenplus</groupId>
                <artifactId>gmavenplus-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>${version.plugin.shade}</version>
                <configuration>
                    <minimizeJar>true</minimizeJar>
                    <createDependencyReducedPom>true</createDependencyReducedPom>
                    <dependencyReducedPomLocation>${project.build.directory}/dependency-reduced-pom.xml</dependencyReducedPomLocation>
                    <useDependencyReducedPomInJar>true</useDependencyReducedPomInJar>
                    <transformers>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheLicenseResourceTransformer" />
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ApacheNoticeResourceTransformer">
                            <addHeader>false</addHeader>
                        </transformer>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.IncludeResourceTransformer">
                            <!-- needed to add the license back in -->
                            <resource>META-INF/LICENSE</resource>
                            <file>${project.basedir}/../LICENSE</file>
                        </transformer>
                    </transformers>
                    <artifactSet>
                        <excludes>
                            <!-- user must manage core module -->
                            <exclude>com.qualityminds.lazyval:lazyval</exclude>
                            <!-- compile-time annotations aren't needed -->
                            <exclude>org.jetbrains:annotations</exclude>
                            <exclude>org.jspecify:jspecify</exclude> <!-- transitive from utils -->
                            <!-- provided by user/buildtool -->
                            <exclude>org.jetbrains.kotlin:*</exclude>
                            <exclude>com.google.devtools.ksp:*</exclude>
                        </excludes>
                    </artifactSet>
                    <relocations>
                        <relocation>
                            <pattern>com.palantir.javapoet</pattern>
                            <shadedPattern>com.qualityminds.lazyval.shaded.javapoet</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>com.squareup.kotlinpoet</pattern>
                            <shadedPattern>com.qualityminds.lazyval.shaded.kotlinpoet</shadedPattern>
                        </relocation>
                    </relocations>
                    <filters>
                        <filter>
                            <artifact>com.palantir.javapoet:*</artifact>
                            <excludes>
                                <exclude>META-INF/**</exclude>
                                <exclude>module-info.class</exclude>
                            </excludes>
                        </filter>
                        <filter>
                            <artifact>com.squareup:*</artifact>
                            <excludes>
                                <exclude>META-INF/**</exclude>
                                <exclude>module-info.class</exclude>
                            </excludes>
                        </filter>
                        <filter>
                            <artifact>com.qualityminds.lazyval:lazyval-utils</artifact>
                            <excludes>
                                <exclude>META-INF/**</exclude>
                            </excludes>
                        </filter>
                    </filters>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>publish</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jetbrains.dokka</groupId>
                        <artifactId>dokka-maven-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>static-analysis</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>${version.plugin.antrun}</version>
                        <executions>
                            <execution>
                                <id>detekt</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target name="detekt">
                                        <property name="detekt.jar" refid="maven.plugin.classpath" />
                                        <java taskname="detekt" dir="${basedir}" fork="true" failonerror="true" classname="dev.detekt.cli.Main">
                                            <classpath>
                                                <path refid="maven.plugin.classpath" />
                                            </classpath>
                                            <arg value="--input" />
                                            <arg value="${basedir}/src/main/kotlin" />
                                            <arg value="--config" />
                                            <arg value="${basedir}/detekt.yml" />
                                            <arg value="--build-upon-default-config" />
                                            <arg value="--report" />
                                            <arg value="sarif:${project.build.directory}/detekt.sarif" />
                                        </java>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                        <dependencies>
                            <dependency>
                                <groupId>dev.detekt</groupId>
                                <artifactId>detekt-cli</artifactId>
                                <version>${version.checks.detekt}</version>
                                <classifier>all</classifier>
                                <type>jar</type>
                                <exclusions>
                                    <!-- Exclude all transitive dependencies since the -all JAR is self-contained -->
                                    <!-- see https://github.com/detekt/detekt/issues/8626 -->
                                    <exclusion>
                                        <groupId>*</groupId>
                                        <artifactId>*</artifactId>
                                    </exclusion>
                                </exclusions>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>